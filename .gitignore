target/
*.exe

# Internal work plans must not be published with the public moonproto crate
# snapshot.
docs/delphi_threading_rewrite_plan.md

# Raw protocol reference is internal maintainer documentation. The public crate
# documents Active Lib/API behavior under docs/api/.
docs/commands/
